// Single entry re-export for Expo to find the router entrypoint
// This file is required so `node_modules/expo/AppEntry.js` can import the
// project's default App module (../../App).

export { default } from 'expo-router/entry';
